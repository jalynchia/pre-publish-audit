---
title: "pre-publish-audit"
abstract: "代码上传发布或分享前的本地安全扫描工具，可自动检测 API 密钥、密码、敏感个人信息以及是否缺少基础规范文件。"
date: 2026-06-26
keywords: ["安全", "审计", "扫描器", "发布前检查"]
---

# 代码发布前安全审计

在把代码公开、上传 GitHub 或发给其他人之前，建议先用这个脚本扫描一下。它能帮你找出不小心遗留的 API 密钥、密码、个人敏感信息（如邮箱、绝对路径等），并检查项目里是否缺少常规的说明文件。

# 它能帮你检查什么

- **敏感凭证**：自动检测 AWS 密钥、大模型 API Key、数据库连接串、密码、JWT 等。
- **个人隐私**：识别邮箱地址、国内手机号、身份证号、内网 IP，以及会泄露系统用户名的绝对路径。
- **文件与结构**：检查项目里是不是漏掉了 `README.md`、`LICENSE`、`.gitignore` 或依赖声明文件（如 `requirements.txt`、`package.json`）。
- **可疑文件**：找出不该上传的 `.env`、`.DS_Store` 以及各种密钥文件（`.pem`、`.key` 等）。

因为只用 Python 3 标准库实现，所以**没有任何第三方依赖**，直接运行即可。
